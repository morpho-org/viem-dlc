// Vendored from https://github.com/internet-development/www-sacred/blob/a8f5f38832555e420b4801f4094b7bf16aa73d6a/components/Divider.tsx
// MIT © Internet Development Studio Company. Generated by playground/scripts/vendor-srcl.mjs — do not edit.
// @ts-nocheck
import styles from '@components/Divider.module.css';

import * as React from 'react';

interface DividerProps extends React.HTMLAttributes<HTMLSpanElement> {
  children?: React.ReactNode;
  type?: string | any;
  style?: any;
}

const Divider: React.FC<DividerProps> = ({ children, style, type }) => {
  if (type === 'GRADIENT') {
    return <div className={styles.gradient} style={style} />;
  }

  if (type === 'DOUBLE') {
    return (
      <div className={styles.divider} style={style}>
        <div className={styles.line} style={{ marginBottom: `2px` }} />
        <div className={styles.line} />
      </div>
    );
  }

  return (
    <div className={styles.divider} style={style}>
      <div className={styles.line} />
    </div>
  );
};

export default Divider;
